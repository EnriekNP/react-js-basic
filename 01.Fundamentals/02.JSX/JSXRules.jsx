const JSXRules = () => {
    return (
        <div>
            <h1>JSX Rules</h1>
            <p>JSX must return a <b>single parent element</b></p>
            <p>JSX element must be<b>properly closed</b></p>
            <p>JSX attribute are written using <b>camelCase</b> (eg., className instead of class)</p>
        </div>
    )
}

export default JSXRules